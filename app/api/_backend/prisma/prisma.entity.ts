import {
  User,
  Item,
  Shoe,
  Role,
  Audit,
  CustomTag,
  Expedition,
} from "@prisma/client";

export type {
  User as UserEntity,
  Item as ItemEntity,
  Shoe as ShoeEntity,
  Role as RoleEntity,
  Audit as AuditEntity,
  CustomTag as CustomTagEntity,
  Expedition as ExpeditionEntity,
};
